-- Drop the old per-cashier names and addresses pool tables.
-- Replaced by the Name Lists system (name_lists, name_list_cashiers, name_list_names).

DROP TABLE IF EXISTS "names";
DROP TABLE IF EXISTS "addresses";
